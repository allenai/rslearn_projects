gcsfs>=2025.2.0
prometheus-client>=0.21.1
python-dotenv>=1.0
PyYAML>=6
rslearn>=0.1.14
s3fs>=2025.2.0
wandb>=0.21
# 3.3.0 mis-validates the shard index crc32c on read (computes it over the
# wrong byte span), so it cannot read stores it wrote itself. Data and stored
# checksums are correct; only 3.3.0's reader is broken. Unpin when fixed.
zarr>=3,<3.3
