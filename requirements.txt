# gcsfs 2026.8.0 returns wrong bytes for ranged reads, which surfaces as a
# bogus Zarr shard-index checksum mismatch on sharded stores. The stored data
# and checksums are correct; only this client is broken. Unpin when fixed.
gcsfs>=2025.2.0,<2026.8
prometheus-client>=0.21.1
python-dotenv>=1.0
PyYAML>=6
rslearn>=0.1.14
s3fs>=2025.2.0
wandb>=0.21
zarr>=3
